# 우리집 가계부 — 진행 현황 (단일 체크리스트)

> 갱신: 2026-05-17
> **이 문서 하나만 보면 됩니다.** 다른 문서는 막힐 때 참조용.

---

## 📌 본인 환경 (확정값)

| 항목 | 값 |
|---|---|
| 도메인 | `bank.hambodev.work` |
| 루트 도메인 | `hambodev.work` (Resend 검증용) |
| 서비스 URL | `https://bank.hambodev.work` |
| GitHub 레포 | https://github.com/sld56/HomeAccountBook |
| 미니 PC OS | Windows + WSL2 + Docker Desktop |
| 인증 방식 | 이메일 매직 링크 (Resend로 발송) |
| 권한 | 2단계 (owner / member) |

---

## ✅ 이미 끝난 것

- [x] M0~M5 코드 완성 (앱 + 인프라 + Edge Functions)
- [x] GitHub 레포 생성 + push
- [x] CI 파이프라인 작성 (.github/workflows/docker-publish.yml)
- [x] docker-compose 이미지 pull 방식으로 전환
- [x] 도메인 결정 (`bank.hambodev.work`)
- [x] 미니 PC에 프로젝트 clone (WSL `/home/sld56/gagyebu/`)
- [x] `gen-keys.mjs` 실행해서 키 생성 (.env 작성 중)

---

## 🟦 지금 해야 할 일 (순서대로)

### 1️⃣ .env 작성 마무리

WSL 터미널에서:
```bash
cd ~/gagyebu/infra
nano .env
```

또는 VS Code Remote-WSL: `code ~/gagyebu/infra/.env`

**필수 채울 항목:**
```env
SITE_URL=https://bank.hambodev.work

# gen-keys.mjs 출력 그대로:
JWT_SECRET=<64글자 hex>
POSTGRES_PASSWORD=<24글자>
REALTIME_SECRET=<64글자 hex>
ANON_KEY=eyJhbGc...
SERVICE_ROLE_KEY=eyJhbGc...

# SMTP는 §3 끝나야 채울 수 있음 — 일단 비워두기
```

저장: `Ctrl+O` → `Enter` → `Ctrl+X`

확인:
```bash
cat ~/gagyebu/infra/.env | head -10
```
값이 다 들어있고 `change_me_...` 없어야 OK.

---

### 2️⃣ GitHub Secrets 2개 등록 (3분)

[👉 Secrets 등록 페이지로 바로 가기](https://github.com/sld56/HomeAccountBook/settings/secrets/actions)

**New repository secret** 2번 클릭, 각각:

| Name | Value |
|---|---|
| `VITE_SUPABASE_URL` | `https://bank.hambodev.work` |
| `VITE_SUPABASE_ANON_KEY` | gen-keys.mjs가 출력한 `ANON_KEY` 값 그대로 |

⚠️ JWT_SECRET이나 SERVICE_ROLE_KEY는 절대 GitHub Secrets에 넣지 마세요. 그건 .env에만.

---

### 3️⃣ Cloudflare Tunnel 설정 (15분)

#### 3.1 Tunnel 생성 + DNS 연결
미니 PC PowerShell(관리자):
```powershell
cloudflared tunnel login
cloudflared tunnel create gagyebu
cloudflared tunnel route dns gagyebu bank.hambodev.work
```

또는 Cloudflare Zero Trust 대시보드의 **Public Hostname**에서 만들었다면 이 단계는 이미 끝.

#### 3.2 config.yml 작성
`%USERPROFILE%\.cloudflared\config.yml`:
```yaml
tunnel: gagyebu
credentials-file: C:\Users\sld12\.cloudflared\<UUID>.json
ingress:
  - hostname: bank.hambodev.work
    service: http://localhost:8080
  - service: http_status:404
```
`<UUID>`는 `cloudflared tunnel list`로 확인.

#### 3.3 Windows 서비스로 등록
```powershell
cloudflared service install
Start-Service cloudflared
Get-Service cloudflared
```
Status = Running이면 OK.

---

### 4️⃣ Resend 메일 설정 (15분)

1. [resend.com](https://resend.com) 가입
2. **Domains → Add Domain** → `hambodev.work` (⚠️ 루트 도메인, `bank.`은 빼고)
3. Resend가 알려주는 DNS 레코드(MX + SPF TXT + DKIM TXT)를 **Cloudflare DNS**에 추가
4. Resend에서 "Verified" 뜰 때까지 대기 (보통 5분)
5. **API Keys → Create API Key** → Permission: Sending → 키 복사 (`re_xxxxx...`)
6. `.env`에 추가:
   ```env
   SMTP_HOST=smtp.resend.com
   SMTP_PORT=587
   SMTP_USER=resend
   SMTP_PASSWORD=re_xxxxxxxxxxxxxxxxxx
   SMTP_ADMIN_EMAIL=noreply@hambodev.work
   SMTP_SENDER_NAME=우리집 가계부
   ```

---

### 5️⃣ GitHub Actions로 첫 빌드 트리거 (5~7분)

[👉 Actions 탭으로 바로 가기](https://github.com/sld56/HomeAccountBook/actions)

1. 좌측에 **"Build and Publish Docker Image"** 클릭
2. 우측 **Run workflow** 버튼 → `main` 브랜치 → **Run workflow** 확인
3. 페이지에서 빌드 진행 보임 (5~7분)
4. ✅ 초록색 체크 = `ghcr.io/sld56/homeaccountbook-web:latest` 이미지 준비 완료

빌드 결과는 [Packages 탭](https://github.com/sld56?tab=packages)에서 확인.

---

### 6️⃣ ghcr.io PAT 발급 + 미니 PC 로그인 (5분)

[👉 PAT 발급 페이지](https://github.com/settings/tokens/new)
- **Note**: `gagyebu-mini-pc-pull`
- **Expiration**: 1 year
- **Scopes**: `read:packages` 만 체크
- **Generate token** → 키 복사 (한 번만 표시됨)

WSL 터미널에서:
```bash
echo "<복사한_PAT>" | docker login ghcr.io -u sld56 --password-stdin
```
`Login Succeeded` 떠야 정상.

---

### 7️⃣ Docker 컨테이너 기동 (5분)

WSL 터미널:
```bash
cd ~/gagyebu/infra
docker compose up -d
# 8개 컨테이너 pull + 기동 (2~3분)

docker compose ps
# 모두 healthy/Up 상태인지 확인
```

문제 있으면:
```bash
docker compose logs -f web
docker compose logs -f gotrue
```

---

### 8️⃣ DB 스키마 적용 (1분)

```bash
docker compose exec -T postgres psql -U postgres -d postgres < ../supabase/migrations/0001_init.sql
docker compose exec -T postgres psql -U postgres -d postgres < ../supabase/migrations/0002_rls.sql
```

확인:
```bash
docker compose exec postgres psql -U postgres -d postgres -c "\dt public.*"
# households, household_members, transactions, ... 9개 표시
```

---

### 9️⃣ 첫 접속 + 본인 가족 만들기 (5분)

브라우저에서 `https://bank.hambodev.work` 접속:
1. 이메일 입력 → "코드 받기"
2. 메일함에서 6자리 코드 확인 → 입력
3. "새 가족 만들기" → 가족 이름·표시 이름·색상 선택
4. 거래 1건 입력해서 동작 확인

---

### 🔟 가족 초대 (가족 인원수만큼 반복)

설정 → "가족 구성원 (서버)" → 이메일 입력 → 초대 → 가족이 메일 링크 클릭 → 합류.

---

## 🟧 나중에 (서비스 안정화 후 1~2주 안에)

### 11️⃣ 백업 자동화
- [age.exe](https://github.com/FiloSottile/age/releases) 설치
- `age-keygen -o C:\backup-keys\gagyebu.key`
- `.env`에 `BACKUP_DIR=C:/Backups/gagyebu`, `BACKUP_AGE_RECIPIENT=age1...` 추가
- `infra/scripts/backup.ps1` 한 번 실행해 동작 확인
- Task Scheduler에 매일 03:00 등록
- ⚠️ age 비밀키 USB·클라우드에 별도 백업

### 12️⃣ Uptime Kuma 모니터링
```bash
docker run -d --restart=always -p 127.0.0.1:3001:3001 \
  -v uptime-kuma:/app/data --name uptime-kuma louislam/uptime-kuma:1
```
브라우저로 `http://localhost:3001` → 모니터 추가 → 알림 설정

---

## 🆘 막힐 때 어떤 정보를 알려주실지

다음 3개만 알려주시면 그 자리에서 풀어드립니다:
1. **몇 번 단계** (1~12)
2. **어떤 명령**을 쳤는지
3. **어떤 메시지**가 떴는지

---

## 🔧 자주 쓰는 명령

```bash
# 상태 확인
cd ~/gagyebu/infra && docker compose ps

# 로그
docker compose logs -f gotrue        # Auth 로그
docker compose logs -f web           # nginx 로그
docker compose logs -f postgres      # DB 로그

# 재시작
docker compose restart gotrue

# 업데이트 (코드 변경 후)
docker compose pull
docker compose up -d
```

```powershell
# Cloudflare Tunnel 상태
Get-Service cloudflared

# Tunnel 로그
Get-EventLog -LogName Application -Source cloudflared -Newest 10
```

---

## 📚 자세한 절차가 필요할 때 (참조용)

| 영역 | 문서 |
|---|---|
| Windows 미니 PC 셋업 상세 | [infra/SETUP-WINDOWS.md](../infra/SETUP-WINDOWS.md) |
| CI/이미지 빌드 흐름 상세 | [infra/CI-IMAGES.md](../infra/CI-IMAGES.md) |
| 보안/DB/RLS 설계 | [docs/05-서버-보안-기획.md](05-서버-보안-기획.md) |
| 기능 사양 | [docs/01-사양서.md](01-사양서.md) |
