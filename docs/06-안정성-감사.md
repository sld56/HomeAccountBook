# 06. 안정성 감사 punch list

> 작성: 2026-05-18
> 범위: 서버 모드(M5) + 풀 CRUD 통합 직후 전체 안정성 점검
> 트리거: 사용자 보고 "보다보니 문제가 너무 많아서 도커로 빌드하는건 후순위로 하고 일단 프로젝트 안정성부터 끓어올려보자"

이 문서는 **수정 전 진단표**입니다. 항목별로 `[ ]` 체크박스를 두어 진행 추적이 가능합니다.

---

## 🔥 P0 — 데이터 정확성 & 치명적 모델 결함

### 1. 멤버·계좌 ID 모델 충돌 (서버 모드 사실상 작동 불가)
- [ ] **`src/stores/memberStore.ts:18`** — 시드 4명(`appa`, `eomma`, `deahyun`, `jiwon`)을 서버 모드 여부 무관하게 채움. `skipHydration`도 없음.
- [ ] **`src/stores/serverSync.ts` 전체** — `household_members` 동기화 누락. 다른 PC에서 가족 추가/탈퇴해도 본 PC 갱신 안 됨.
- [ ] **`src/stores/txServer.ts:19`** — `member_id: tx.member || user.id`. `tx.member`가 `'appa'` 같은 비-UUID이면 PostgreSQL UUID 캐스팅 오류.
- [ ] **`src/stores/txServer.ts:20`** — `account_id: tx.account || null`. `'kb-main'` 같은 시드 ID는 FK 위반.
- [ ] **`src/components/domain/TransactionRow.tsx:2, :21`** — 정적 `MEMBERS_BY_ID` import. 서버 멤버 이름 표시 불가, 새 멤버 추가 시 거래에 빈 이름.
- [ ] **`src/features/transactions/TransactionForm.tsx:41-42`** — `members[0]?.id`로 default. 멤버 sync 실패 시 `member=''`, zod 검증 실패에 빈 상태 안내 없음.

**해결 방향**: memberStore를 서버 모드에서 빈 배열로 시작 → `serverSync`에 `household_members` fetch + Realtime 추가 → 모든 거래 입력 폼이 동적 멤버 사용 → TransactionRow도 `useMembers` 훅 사용.

---

### 2. `CURRENT_YM = '2026-05'` 하드코딩 3곳
- [ ] **`src/features/dashboard/DashboardPage.tsx:25, :32, :117, :308`** — 6월 진입 즉시 KPI/카테고리/가족 통계가 5월에 멈춤.
- [ ] **`src/features/budget/BudgetPage.tsx:16, :36, :51, :65`** — 예산 진행률·페이지 헤더 5월 고정.
- [ ] **`src/features/reports/tabs/FamilyOverview.tsx:14, :21`** — "가족 통합" 탭이 5월에 정지.

**해결 방향**: `lib/format.ts`에 `currentYm()` 헬퍼 추가 → 3곳에서 `useMemo(() => currentYm(), [])` 사용.

---

### 3. 시드 패턴이 5개 스토어에 잔존
- [ ] **`src/stores/transactionStore.ts:19`** — `isServerConfigured ? [] : DEV ? seedTransactions : []`. persist 미들웨어가 이전 로컬값을 다시 채워 서버 모드 진입 직후 시드 깜빡임.
- [ ] **`src/stores/accountStore.ts:17`** — 동일 패턴.
- [ ] **`src/stores/budgetStore.ts:16`** — 동일.
- [ ] **`src/stores/goalStore.ts:17`** — 동일.
- [ ] **`src/stores/upcomingStore.ts:17`** — 동일.
- [ ] **`src/data/monthly.ts`** — 이제 어디서도 import 안 되지만 파일 잔존. 삭제 또는 `// @deprecated`.

**해결 방향**: 서버 모드에서 모든 store가 `skipHydration: true` + 초기 빈 배열 + serverSync가 채우는 단방향 흐름 보장.

---

### 4. MigrationModal 가드가 너무 약함
- [ ] **`src/features/settings/MigrationModal.tsx:34`** — `if (txCount > 26)` 시드 정확히 26건일 때만 건너뜀. 1건 수정/추가/삭제로 시드가 서버 가족 가계부에 import됨.
- [ ] **`src/features/settings/MigrationModal.tsx:43-55`** — 거래만 import, accounts/budgets/goals/upcoming 누락.

**해결 방향**: 시드 데이터에 마커(`__isSeed: true`) 추가 또는 사용자 명시적 "마이그레이션" 버튼만 허용 (자동 모달 제거).

---

## ⚠️ P1 — 크래시·NaN·빈 상태·폼 버그

### 5. React 안티패턴
- [ ] **`src/features/settings/AccountsSection.tsx:103-114`** — `useState(() => {...})`로 초기값만 잡고 `useEffect` 동기화 없음. 다른 계좌 편집 클릭 시 직전 폼 값 잔존.
- [ ] **`src/features/budget/BudgetPage.tsx:323-332, :449-458`** — `useMemo`를 effect처럼 사용 (setTitle/setSaved). Goal/Upcoming 모달 첫 오픈 시 빈 폼 가능.
- [ ] **`src/features/transactions/TransactionForm.tsx:36-43`** — state 초기값 `initial?.xxx`만 잡음. 행 바꿔가며 편집 시 직전 값 유지 위험.

### 6. 빈 상태 처리
- [ ] **`src/components/charts/Sparkline.tsx:17-19`** — 모두 0이면 그래프가 바닥에 깔림, "데이터 없음" 안내 없음.
- [ ] **`src/components/charts/MonthlyBars.tsx:15`** — 모두 0이면 막대 0px, 빈 메시지 없음.
- [ ] **`src/components/charts/YearlyLine.tsx:20`** — 동일 패턴.
- [ ] **`src/features/reports/tabs/Yearly.tsx:21-29`** — 거래 0건 가구에서 KPI 모두 "0원 · 0.0%", 베스트·워스트가 같은 0월.
- [ ] **`src/features/dashboard/DashboardPage.tsx:145, :182, :262`** — upcoming/accounts/cats 0개일 때 빈 카드만, 안내 없음.

### 7. 계산 오류
- [ ] **`src/features/dashboard/DashboardPage.tsx:39-42`** — `pctDelta(curr, 0)`이 `curr === 0 ? 0 : 100`. 신규 가구 첫 달 입력이 모두 "전월 대비 +100%"로 표시.
- [ ] **`src/features/reports/tabs/FamilyOverview.tsx:48`** — prev=0이면 0%. "전월 ▲ 0%"로 변화 없음처럼 잘못 표시.
- [ ] **`src/components/ui/ProgressBar.tsx:18`** — max=0이면 Infinity → clamp로 100%. Dashboard:369(Goal)에서 가드 없이 사용.
- [ ] **`src/features/transactions/TransactionForm.tsx:41`** — `members[0]?.id` 멤버 0명 케이스 가드 없음.

---

## 🔄 P2 — 서버 동기화 불일치

- [ ] **`src/stores/serverSync.ts`** — members 동기화 누락 (P0 #1과 겹침).
- [ ] **`src/components/layout/AppShell.tsx:23-29`** — cleanup이 `if (household_id)` 분기 안에만. household_id가 null로 바뀌면 `stopServerSync` 호출 안 됨 → 채널 중복 누적.
- [ ] **`src/features/auth/authStore.ts:37-47`** — `onAuthStateChange` 리스너 해제 없음. HMR/StrictMode에서 중복 등록.
- [ ] **`src/features/auth/authStore.ts:55-76`** — `refreshMembership`이 manual. 다른 PC에서 멤버십 추가/탈퇴 시 본 PC 모름 — 추방 후에도 화면 정상.
- [ ] **`src/features/settings/FamilySection.tsx:39-57`** — `load()` manual, Realtime 구독 없음.
- [ ] **`src/stores/budgetServer.ts:8-17`** — `onConflict: 'household_id,cat,ym'`인데 PostgreSQL은 NULLS DISTINCT가 기본. ym=NULL이면 충돌 없이 중복 row 생성.
- [ ] **`src/features/auth/authStore.ts:78-87`** — `signOut`이 zustand persist 키 정리 안 함. 다른 가족이 같은 브라우저 사용 시 이전 데이터 잠깐 노출.

---

## 📝 P3 — 입력 검증

- [ ] **`src/features/transactions/TransactionSchema.ts:11`** — date regex만 검사. `2026-02-30` 같은 잘못된 날짜 통과 → DB가 거절.
- [ ] **`src/features/transactions/TransactionSchema.ts:7`** — `title.max(40)`만, trim 없음. 공백 40자 통과.
- [ ] **`src/features/transactions/TransactionSchema.ts:5`** — `int()`로 소수 거절되나 에러 메시지 모호 ("금액을 입력해주세요").
- [ ] **`src/features/budget/BudgetPage.tsx:236-253`** — 예산 빈 칸 = 자동 onDelete. "0으로 설정" 의도와 헷갈림.
- [ ] **`src/features/budget/BudgetPage.tsx:340-350`** — Goal `saved`가 `target` 초과 가능.
- [ ] **`src/features/budget/BudgetPage.tsx:466`** — Upcoming에 과거 날짜 입력 허용.
- [ ] **`src/features/settings/AccountsSection.tsx:124`** — 정규식 `Number(balance.replace(/[-]?[^0-9-]/g, ''))` 비정상. "1-000" → "1000" 강제.
- [ ] **`src/features/settings/AccountsSection.tsx:122-123`** — 라벨 길이 상한 없음.
- [ ] **`src/features/settings/SettingsPage.tsx:148-156`** — 가족 추가 폼 maxLength·중복 검사 없음, confirm 없음.
- [ ] **`src/features/settings/SettingsPage.tsx:127`** — 마지막 1명은 못 지우지만, 지운 멤버의 기존 거래는 그대로. 매핑 끊김.
- [ ] **`src/features/onboarding/OnboardingPage.tsx:91, :172`** — `maxLength`만, minLength·trim 없음. 공백 1자 가족 이름 가능.
- [ ] **`src/features/onboarding/OnboardingPage.tsx:170-172`** — 초대 토큰 `pattern` 검증 없음.

---

## 🪶 P4 — UX·보안·성능

- [ ] **`src/lib/supabase.ts:15-26`** — URL/anonKey 누락 시 placeholder. 명시적 throw가 안전.
- [ ] **`src/features/settings/FamilySection.tsx:156`** — `clipboard.writeText` 에러 무시.
- [ ] **`src/features/transactions/TransactionForm.tsx:117`** — `confirm()` 네이티브. UI 톤 불일치.
- [ ] **`src/features/budget/BudgetPage.tsx:366, :481`** — 동일 `confirm()`.
- [ ] **`src/features/dashboard/DashboardPage.tsx:301-355`** — FamilyVariant: 가족 0~3명일 때 `grid cols-4` 레이아웃 깨짐.
- [ ] **`src/components/domain/TransactionRow.tsx`** — `accounts.find(...)` 매 행 O(n). useMemo로 map화.
- [ ] **`src/stores/transactionStore.ts:66-67`** — `skipHydration: isServerConfigured`이나 set마다 storage write. 로그아웃 시 미정리.
- [ ] **`src/features/settings/MigrationModal.tsx:30`** — `MIGRATED_MARKER`만 LocalStorage. 가족 갈아타면 또 뜸.
- [ ] **`src/components/charts/StackBar.tsx:62-68`** — ChartTooltip x 계산이 `% + ratio*50` 단위 혼합. 툴팁 위치 어긋날 가능성.
- [ ] **`src/data/monthly.ts`** — 사용 안 됨, 삭제 또는 deprecate.

---

## 권장 수정 순서

1. **세션 1 (P0 일괄)**: 멤버/계좌 ID 모델 재설계 + CURRENT_YM 제거 + 시드 패턴 통일 + MigrationModal 강화.
2. **세션 2 (P1)**: 폼 안티패턴 + 빈 상태 처리 + 계산 오류.
3. **세션 3 (P2)**: 서버 동기화 일관성 + 로그아웃 정리.
4. **세션 4 (P3 + P4)**: 입력 검증 + UX/성능 정리.

각 세션 끝마다 `npm run typecheck && npm run lint && npm run build`, 가능하면 미니 PC 실서버에서 시나리오 검증 (신규 가족 가입 → 거래 추가 → 다른 PC에서 보이기).
